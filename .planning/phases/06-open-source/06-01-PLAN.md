---
phase: 06-open-source
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - .github/workflows/build.yml
autonomous: true
requirements: [OSS-02]
must_haves:
  truths:
    - "`./gradlew :app:lintDebug` completes without IncompatibleClassChangeError"
    - "CI workflow file exists at canonical path and uses pinned action versions"
    - "CI workflow triggers on push to `main` and on PRs to `main`"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "Lint configuration that disables the NullSafeMutableLiveData check"
      contains: 'disable += "NullSafeMutableLiveData"'
    - path: ".github/workflows/build.yml"
      provides: "GitHub Actions CI: unit tests + lint + debug APK build + artifact upload"
      contains: "Android CI"
  key_links:
    - from: ".github/workflows/build.yml"
      to: "app/build.gradle.kts"
      via: "`./gradlew lint` step depends on the lint disable fix"
      pattern: 'disable \+= "NullSafeMutableLiveData"'
---

<objective>
Fix the pre-existing lint crash AND create the GitHub Actions CI workflow so CI is green from day one.

Purpose: CI cannot be activated until the lint crash is fixed, otherwise the build badge in README will be permanently red. Both changes are pure additive file edits with no overlap, so they land in the same plan.

Output:
- `app/build.gradle.kts` updated with a `lint { disable += "NullSafeMutableLiveData" }` block inside `android { }`
- `.github/workflows/build.yml` created with the canonical pinned Android CI workflow
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-open-source/06-CONTEXT.md
@.planning/phases/06-open-source/06-RESEARCH.md
@app/build.gradle.kts

<interfaces>
Canonical pinned action versions (from RESEARCH.md §Standard Stack — verified 2026-04-12):
- actions/checkout@v4
- actions/setup-java@v4 (Temurin, JDK 17)
- gradle/actions/setup-gradle@v4
- actions/upload-artifact@v4

Root cause of lint crash (RESEARCH.md §BLOCKING: Lint Crash Fix):
AGP 8.7.3 + lifecycle 2.8.7 — lint jar compiled against older Kotlin Analysis API where `KaCallableMemberCall` was a class; now an interface → `IncompatibleClassChangeError` in `NonNullableMutableLiveDataDetector` (lint ID: `NullSafeMutableLiveData`). Do NOT bump AGP, Kotlin, or lifecycle — out of scope.

Project JVM target (confirmed via existing build.gradle.kts): JavaVersion.VERSION_17 / jvmTarget = "17". Temurin distribution matches.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lint disable block to app/build.gradle.kts</name>
  <files>app/build.gradle.kts</files>
  <read_first>
    - app/build.gradle.kts (to locate the existing `android { }` block and place the lint config consistently with existing config order)
    - .planning/phases/06-open-source/06-RESEARCH.md §BLOCKING: Lint Crash Fix (for the exact fix rationale)
  </read_first>
  <action>
    Inside the existing `android { }` block in `app/build.gradle.kts`, add a `lint` configuration block that disables the `NullSafeMutableLiveData` check.

    Exact Kotlin DSL to add (place after existing `buildTypes { }` block, before `compileOptions { }`, or at the end of `android { }` — whichever keeps existing ordering consistent):

    ```kotlin
    lint {
        disable += "NullSafeMutableLiveData"
    }
    ```

    DO NOT:
    - Bump AGP, Kotlin, or lifecycle versions (explicitly out of scope per RESEARCH.md — risky end-to-end retest).
    - Disable all lint checks via `abortOnError = false` (loses all lint signal).
    - Use `lintOptions { }` (deprecated DSL; `lint { }` is the current form for AGP 8.x).
    - Add any other lint configuration unless fixing a new crash.

    WHY the specific string `"NullSafeMutableLiveData"`: this is the lint check ID (the detector class is `NonNullableMutableLiveDataDetector` but the registered issue ID used by `disable` is `NullSafeMutableLiveData`). Verified in RESEARCH.md §Code Examples.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/android-audio-controler && ./gradlew :app:lintDebug 2>&1 | tail -20 | grep -Ev "IncompatibleClassChangeError|UNEXPECTED TOP-LEVEL EXCEPTION" &gt; /dev/null &amp;&amp; ./gradlew :app:lintDebug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'disable += "NullSafeMutableLiveData"' app/build.gradle.kts` returns a match inside the `android { lint { } }` block.
    - `./gradlew :app:lintDebug` exits 0 and its output contains neither `IncompatibleClassChangeError` nor `UNEXPECTED TOP-LEVEL EXCEPTION`.
    - `./gradlew :app:testDebugUnitTest` still passes (regression check — existing tests unchanged).
  </acceptance_criteria>
  <done>Lint runs cleanly locally; the CI `./gradlew lint` step in Task 2 will not crash.</done>
</task>

<task type="auto">
  <name>Task 2: Create .github/workflows/build.yml with canonical pinned Android CI</name>
  <files>.github/workflows/build.yml</files>
  <read_first>
    - .planning/phases/06-open-source/06-RESEARCH.md §Architecture Patterns / Pattern 1 and §Code Examples (for the exact verified YAML)
    - app/build.gradle.kts (to confirm `compileOptions`/`kotlinOptions` use JDK 17)
  </read_first>
  <action>
    Create the directory `.github/workflows/` if it does not exist, then write `.github/workflows/build.yml` with EXACTLY the following content (verbatim — no substitutions, no renames):

    ```yaml
    name: Android CI

    on:
      push:
        branches: [ "main" ]
      pull_request:
        branches: [ "main" ]

    jobs:
      build:
        runs-on: ubuntu-latest

        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Set up JDK 17
            uses: actions/setup-java@v4
            with:
              java-version: '17'
              distribution: 'temurin'

          - name: Setup Gradle
            uses: gradle/actions/setup-gradle@v4

          - name: Make gradlew executable
            run: chmod +x ./gradlew

          - name: Run unit tests
            run: ./gradlew test

          - name: Run lint
            run: ./gradlew lint

          - name: Build debug APK
            run: ./gradlew assembleDebug

          - name: Upload APK artifact
            uses: actions/upload-artifact@v4
            with:
              name: app-debug
              path: app/build/outputs/apk/debug/app-debug.apk
              retention-days: 30
    ```

    DO NOT:
    - Add a separate `actions/cache` step for Gradle (redundant with `setup-gradle@v4`, causes key collisions — RESEARCH.md §Anti-Patterns).
    - Add `cache: gradle` inside `actions/setup-java@v4` (redundant — setup-gradle handles it).
    - Use `gradle/gradle-build-action` (deprecated alias).
    - Use `softprops/action-gh-release@v3` (released same day as research, not yet battle-tested).
    - Trigger on any branch other than `main` — the repo will be renamed `master → main` in Plan 02 before first push.

    WHY these exact versions: RESEARCH.md §Standard Stack verified 2026-04-12 against each action's official release page. v4 is current stable for checkout/setup-java/setup-gradle/upload-artifact; upload-artifact v3 is deprecated end of 2024.
  </action>
  <verify>
    <automated>test -f .github/workflows/build.yml && grep -q "actions/checkout@v4" .github/workflows/build.yml && grep -q "actions/setup-java@v4" .github/workflows/build.yml && grep -q "gradle/actions/setup-gradle@v4" .github/workflows/build.yml && grep -q "actions/upload-artifact@v4" .github/workflows/build.yml && grep -q 'branches: \[ "main" \]' .github/workflows/build.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/build.yml` exists.
    - File contains all 4 pinned action references above.
    - File triggers on `main` branch only (both push and pull_request).
    - File includes 4 Gradle steps in order: `test`, `lint`, `assembleDebug`, upload-artifact.
    - `yq eval '.jobs.build.steps | length' .github/workflows/build.yml` returns at least 7 (checkout + setup-java + setup-gradle + chmod + test + lint + assembleDebug + upload = 8).
  </acceptance_criteria>
  <done>CI workflow exists at canonical path, references only pinned `@v4` actions, targets `main`, and builds+tests+lints+publishes the debug APK artifact.</done>
</task>

</tasks>

<verification>
Combined verification:
- `./gradlew :app:lintDebug` succeeds (Task 1 outcome)
- `./gradlew :app:testDebugUnitTest :app:lintDebug :app:assembleDebug` — full local smoke matching the CI script
- `.github/workflows/build.yml` parses as valid YAML: `yq eval . .github/workflows/build.yml > /dev/null`
</verification>

<success_criteria>
- Lint no longer crashes: local `./gradlew :app:lintDebug` exits 0 with no `IncompatibleClassChangeError`.
- CI workflow file committed with exact pinned action versions.
- No app source files (non-build) touched — regression surface is zero.
</success_criteria>

<output>
After completion, create `.planning/phases/06-open-source/06-01-SUMMARY.md` noting: which lint check was disabled, confirmation of `./gradlew lint` exit 0, and the 4 pinned action versions committed to `.github/workflows/build.yml`.
</output>
